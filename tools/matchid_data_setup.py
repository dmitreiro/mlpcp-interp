def main():
    # check for all *nodes.csv files in that folder
    # create a folder with parameters for each readed *nodes.csv file
    # save each row of each file in a separate file, inside folder
    pass

if __name__ == "__main__":
    exit(main())